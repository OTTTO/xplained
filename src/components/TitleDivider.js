import { Divider } from "@mui/material";

export function TitleDivider() {
  return (
    <Divider
      sx={{
        borderColor: "rgba(0,0,0,1)",
        borderBottomWidth: 1,
        width: "80%",
        margin: "0 auto",
        opacity: 1,
      }}
    />
  );
}
